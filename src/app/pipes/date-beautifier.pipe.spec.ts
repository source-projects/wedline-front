import { DateBeautifierPipe } from './date-beautifier.pipe';

describe('DateBeautifierPipe', () => {
  it('create an instance', () => {
    const pipe = new DateBeautifierPipe();
    expect(pipe).toBeTruthy();
  });
});
